from vigenere import decrypt, encrypt


def main():
    # main loop

    # based on user's input:
    # 1. save password
    # a) get `name`
    # b) get `pwd`
    # encrypted = encrypt(pwd)
    # save(name, encrypted)

    # 2. get password
    # get `name`
    # encrypted = lookup(name)
    # pwd = decrypt(encrypted)
    # display `name` and `pwd`
    ...


def save(name, pwd):
    return True


def lookup(name):
    return "pwd or 'not found' message"
